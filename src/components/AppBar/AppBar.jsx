import Navigation from "../Navigation/Navigation";
import { Header } from "./AppBar.styled";

const AppBar = () => {
    return (
        <Header>
            <Navigation />
        </Header>     
    )
};

export default AppBar;